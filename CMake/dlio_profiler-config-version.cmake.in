#-----------------------------------------------------------------------------
# Version file for install directory
#-----------------------------------------------------------------------------
set(PACKAGE_VERSION @DLIO_PROFILER_PACKAGE_VERSION@)

if ("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @DLIO_PROFILER_VERSION_MAJOR@)
    set(PACKAGE_VERSION_COMPATIBLE 1)
    if ("${PACKAGE_FIND_VERSION_PATCH}" EQUAL @DLIO_PROFILER_VERSION_PATCH@)
        set(PACKAGE_VERSION_EXACT 1)
    endif ()
endif ()